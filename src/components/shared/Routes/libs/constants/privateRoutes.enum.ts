export enum PrivateRoutes {
  MAIN = "/",
  SELECT_PROJECT = "/select-project",
  DASHBOARD = "/dashboard",
  PROFILE = "/profile",
  SETTINGS = "/settings",
  TEAM = "/team",
  COMPANY = "/company",
}
